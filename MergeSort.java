
public class MergeSort {
    private RunManager runManager; 
    
    public MergeSort(RunManager runManager)
    {
        this.runManager = runManager; 
    }
    
    public void merge()
    {
        int currRun = 0; 
        while (runManager.getNumRuns() > 1)
        {
            // while (currRun < getNumRuns)
                // Get block from first 8 runs
                
                // Fill Min heap
                    // Add run flag to records in heap
                
                // while loop until heap is empty
                        // check if one of the runs is exhausted
                        // 
                // Increment currRun by runs merged
            
            // runManager = recordRuns()
        }
    }
    
    private RunManager recordRuns()
    {
        return null;
    }
    
    

}
